
function cutTress(matrix) {
  //iterate through matrix
    //if value > 1
      //if values on all 4 sides are 0's
        //break
        //return not possible to cut trees aka -1
      //else



  var possibleTpCutAll = true;
  for (var i = 0; i < matrix.length; i++) {
    for (var j = 0; j < matrix[i].length; j++) {
      if ()
    }
  }

}